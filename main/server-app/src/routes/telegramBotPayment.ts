// 'use server'
// export default async function handler(req: NextApiRequest, res: NextApiResponse) {
//   const { data } = req.body;
//   const { user_id, email, amount } = data as IPostDataOnServer

// }